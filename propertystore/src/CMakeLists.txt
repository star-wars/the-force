INCLUDE_DIRECTORIES(../include/)

SET(C4_SRC ${C4_SRC}
  C4.cpp
  C4.tpp
)

ADD_LIBRARY(C4 ${C4_SRC})
TARGET_LINK_LIBRARIES(C4 ZETER ${Boost_LIBRARIES})

INSTALL(TARGETS C4
        RUNTIME DESTINATION bin
        ARCHIVE DESTINATION lib
        LIBRARY DESTINATION lib
        )

